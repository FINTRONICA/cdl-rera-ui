// Backward compatibility file - re-exports everything from the new modular structure
// This ensures existing imports continue to work while we migrate to the new structure

export * from './validation/index';